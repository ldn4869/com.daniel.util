package com.daniel.util.tester;

import java.io.File;

public class RecursiveFileSize {

//  public static long getTotalSize(File file) {
//    if (FileSizeFinder.isFile(file))
//      return FileSizeFinder.length(file);
//    final File[] children = FileSizeFinder.listFiles(file);
//    long total = 0;
//    for (int ix = 0; ix < children.length; ix++) {
//      total += getTotalSize(children[ix]);
//    }
//
//    return total;
//  }
}
